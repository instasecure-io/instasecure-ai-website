---
import UseCaseLayout from '@/layouts/UseCaseLayout.astro';
import FeatureGrid from '@/components/sections/FeatureGrid.astro';
import Video from '@/components/ui/Video.astro';
import SectionHeader from '@/components/sections/SectionHeader.astro';
import { SHARED_PROBLEM, SHARED_SOLUTION, SHARED_WHY_MATTERS } from '@/data/workforce-landing-shared';
---
<UseCaseLayout
  title="Who Really Has Access on AWS? | InstaSecure"
  description="Instant visibility into AWS admins, unused roles, and risky permission chains. Map every IAM access path in minutes — without consultants or spreadsheets."
  eyebrow="Identity Visibility"
  heroTitle="Who Really Has Access? Find Out in Minutes."
  heroDescription="Instant visibility into admins, unused roles, and risky permission chains — without a six-week consulting engagement or a pile of spreadsheets."
>
  <section class="mx-auto max-w-7xl px-6 py-16">
    <Video id="D3pmyxFWmC4" />
  </section>

  <div class="bg-slate-50">
    <section class="mx-auto max-w-4xl px-6 py-20">
      <SectionHeader eyebrow={SHARED_PROBLEM.eyebrow} title={SHARED_PROBLEM.title} subtitle={SHARED_PROBLEM.description} />
      <ul class="max-w-2xl mx-auto mt-10 space-y-3">
        {SHARED_PROBLEM.points.map(p => (
          <li class="flex gap-3 items-start p-5 bg-white rounded-xl border border-black/5">
            <span class="text-[var(--color-accent)] text-xl font-bold leading-none shrink-0">⚠</span>
            <span class="text-[16px] text-slate-800 leading-relaxed">{p}</span>
          </li>
        ))}
      </ul>
    </section>
  </div>

  <FeatureGrid
    eyebrow="The Solution"
    title="Make access visible, then make it smaller"
    subtitle="InstaSecure traces every access path — from IdP to role assumption to AWS API action — so you can see the true reach of every identity."
    features={SHARED_SOLUTION}
    columns={3}
  />

  <div class="bg-slate-50">
    <FeatureGrid
      eyebrow="Why It Matters"
      title="The outcomes that turn blind spots into control"
      features={SHARED_WHY_MATTERS}
      columns={2}
    />
  </div>
</UseCaseLayout>
